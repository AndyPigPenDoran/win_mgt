
CHOICES_TASKS = {
    "shutdown": {},
    "services": {},
    "filesystem": {}
}

# Connection options
CHOICES_PROTOCOLS = ["HTTP", "HTTPS"]
DEFAULT_PROTOCOL = "HTTP"

DEFAULT_PORTS = {
    "HTTP": 5985,
    "HTTPS": 5986
}

CHOICES_MSG_ENCRYPTION = ["auto", "never", "always"]
DEFAULT_MSG_ENCRYPTION = "auto"

